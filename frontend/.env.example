# Local development (copied automatically by Vite in dev mode)
VITE_API_BASE_URL=http://localhost:5000/api/v1

# Production builds use .env.production (Railway API URL). Do not point production at localhost.
VITE_RECAPTCHA_SITE_KEY=
